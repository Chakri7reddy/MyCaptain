def frequency(string):
    d = dict()
    for letter in string:
        if letter not in d:
            d[letter] = 1
        else:
            d[letter] += 1
    return d 
print(frequency(input("string enter : ")))